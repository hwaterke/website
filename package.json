{
  "name": "hwaterke-website",
  "description": "Harold Waterkeyn website",
  "version": "1.0.0",
  "author": "Harold Waterkeyn",
  "license": "MIT",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --single-quote --bracket-spacing=false --no-semi --write \"src/**/*.js\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "precommit": "lint-staged"
  },
  "dependencies": {
    "gatsby": "^1.9.108",
    "gatsby-link": "^1.6.26",
    "gatsby-plugin-react-helmet": "^1.0.8"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lint-staged": "^5.0.0",
    "prettier": "^1.8.2"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write --single-quote --bracket-spacing=false --no-semi",
      "git add"
    ]
  }
}
